extends layoutHome

block content
	h1 bpmn-server Workbench Sample WF Demo
	include includes/dashboardHeader.pug

	script(type="text/javascript").
		$( function() {
			$( "#tabs" ).tabs();
		} );

	div(id="tab13s")
		div(id="processes",style="border-bottom: solid;border-width: thin;")
				include user/userModels.pug
		div(id="tasks")
			include user/userTasks.pug
		div(id="instances")
			include user/userInstances.pug

		div(id='footer',style="padding-bottom:20px;")
	
	